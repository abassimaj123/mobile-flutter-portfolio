name: calcwise_lint
description: Custom lint rules for the calcwise Flutter portfolio
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  analyzer: ">=6.0.0 <8.0.0"
  analyzer_plugin: ">=0.11.0 <0.12.0"
  custom_lint_builder: ">=0.6.0 <0.8.0"

dev_dependencies:
  test: ">=1.24.0 <2.0.0"
  custom_lint: ">=0.6.0 <0.8.0"
